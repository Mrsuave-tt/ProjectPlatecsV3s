<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
    <meta name="mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-capable" content="yes" />
    <meta name="apple-mobile-web-app-status-bar-style" content="black-translucent" />
    <title>@ViewData["Title"] - ProjectPlatec</title>
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/ProjectPlatec.styles.css" asp-append-version="true" />
</head>
<body>
    <header>
        <nav class="navbar navbar-expand-sm navbar-toggleable-sm navbar-light bg-white border-bottom box-shadow mb-3">
            <div class="container-fluid">
                @if (!User.Identity?.IsAuthenticated == true || (!(ViewContext.RouteData.Values["controller"]?.ToString() ?? "").StartsWith("Student") && !(ViewContext.RouteData.Values["controller"]?.ToString() ?? "").StartsWith("Admin")))
                {
                    <a class="navbar-brand" asp-area="" asp-controller="Home" asp-action="Index">ProjectPlatec</a>
                }
                <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target=".navbar-collapse" aria-controls="navbarSupportedContent"
                        aria-expanded="false" aria-label="Toggle navigation">
                    <span class="navbar-toggler-icon"></span>
                </button>
                <div class="navbar-collapse collapse d-sm-inline-flex justify-content-between">
                    @if (User.Identity?.IsAuthenticated == true)
                    {
                        var controllerName = ViewContext.RouteData.Values["controller"]?.ToString() ?? "";
                        var isStudentArea = controllerName.StartsWith("Student");
                        
                        @if (!isStudentArea)
                        {
                            @* Admin/Teacher Navigation *@
                            <ul class="navbar-nav flex-grow-1">
                                @if (User.IsInRole("Admin"))
                                {
                                    <li class="nav-item">
                                        <a class="nav-link text-dark" href="/Admin/Index">Admin Dashboard</a>
                                    </li>
                                }
                                else
                                {
                                    <li class="nav-item">
                                        <a class="nav-link text-dark" href="/Dashboard/Index">Dashboard</a>
                                    </li>
                                }
                                <li class="nav-item">
                                    <a class="nav-link text-dark" href="/Student/Index">Students</a>
                                </li>
                                @if (User.IsInRole("Admin"))
                                {
                                    <li class="nav-item">
                                        <a class="nav-link text-dark" href="/Teacher/Index">Teachers</a>
                                    </li>
                                }
                                <li class="nav-item">
                                    <a class="nav-link text-dark" href="/Attendance/Mark">Mark Attendance</a>
                                </li>
                                @if (User.IsInRole("Admin"))
                                {
                                    <li class="nav-item">
                                        <a class="nav-link text-dark" href="/Admin/Reports">Reports</a>
                                    </li>
                                }
                                else
                                {
                                    <li class="nav-item">
                                        <a class="nav-link text-dark" href="/Dashboard/Reports">Reports</a>
                                    </li>
                                }
                            </ul>
                        }
                        else
                        {
                            @* Student Navigation *@
                            <ul class="navbar-nav flex-grow-1">
                                <li class="nav-item">
                                    <a class="nav-link text-dark" asp-controller="StudentDashboard" asp-action="Index">Dashboard</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link text-dark" asp-controller="StudentDashboard" asp-action="MyAttendance">My Attendance</a>
                                </li>
                                <li class="nav-item">
                                    <a class="nav-link text-dark" asp-controller="StudentAccount" asp-action="Profile">Profile</a>
                                </li>
                            </ul>
                        }
                        <ul class="navbar-nav">
                            <li class="nav-item">
                                <span class="navbar-text me-3">Hello, @User.Identity.Name!</span>
                            </li>
                            <li class="nav-item">
                                <form asp-controller="Account" asp-action="Logout" method="post" class="d-inline">
                                    <button type="submit" class="btn btn-link nav-link text-dark">Logout</button>
                                </form>
                            </li>
                        </ul>
                    }
                    else
                    {
                        <ul class="navbar-nav ms-auto">
                            <li class="nav-item">
                                <a class="nav-link text-dark" asp-controller="Account" asp-action="Login">Login</a>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link text-dark" asp-controller="Account" asp-action="Register"></a>
                            </li>
                        </ul>
                    }
                </div>
            </div>
        </nav>
    </header>
    <div class="container">
        <main role="main" class="pb-3">
            @RenderBody()
        </main>
    </div>

    <footer class="border-top footer text-muted">
        <div class="container">
            &copy; 2026 - ProjectPlatec - <a asp-area="" asp-controller="Home" asp-action="Privacy">Privacy</a>
        </div>
    </footer>
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/dist/js/bootstrap.bundle.min.js"></script>
    <script src="~/js/site.js" asp-append-version="true"></script>
    @await RenderSectionAsync("Scripts", required: false)
</body>
</html>
